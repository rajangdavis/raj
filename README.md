# raj

A vibemaxxed TUI for editing code, running on a forked version of ghostty or
on iTerm2 via a dynamic profile.

I want a low memory editor that could run in the terminal that has sublime text/VS Code style keybindings.

I want to eventually build/integrate my own custom agent harness with integrations within the editor.

This is very experimental and in progress; all of the code was generated by Claude across many sessions.

I am putting this up so I can test drive it; use at your own risk :)

## Setup

You can build the cli with

```
$ go build ./cmd/raj
```

And call it with:

```
$ ./raj <file|directory>
```